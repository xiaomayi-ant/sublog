---
// SiteFooter — 索引页的落款：一行署名 + 一条工具行。
//
// 曾经是照 innei 抄来的 About / More / Contact 三栏。撤掉的理由不是审美：
// 六格里四格是没有去处的占位（Analytics / Monitor / About this project / Send email），
// 「About this site」与主导航的 About 重复，只有外部 GitHub 是独有去处，
// 而那一个已经挪进 /about 正文。六格骨架换一个链接，撑不起页脚这个位置。
// site-direction.md §5 本来写的也是两行落款，三栏从不在这个站的设计里。
//
// 留下的两样各有用处：署名是站点身份在每个索引页的固定出现点（文章页沉浸，
// 用 SiteFooterMinimal），RSS 是这个站唯一的订阅入口。
const year = new Date().getUTCFullYear();
---

<footer class="site-foot site-shell" data-footer="signature">
  <div class="foot-mark">
    <a class="foot-name" href="/">sumoer</a>
    <p class="foot-tagline">Vision: world peace</p>
  </div>

  <div class="foot-utility">
    <p><a href="/rss.xml">RSS</a></p>
    <p>© {year}</p>
  </div>
</footer>

<style>
  .site-foot {
    margin-top: clamp(4rem, 10vw, 7rem);
    padding-bottom: var(--space-lg);
  }

  /* ── 落款 ── */
  .foot-name {
    display: block;
    color: var(--color-ink);
    font-family: var(--font-display);
    font-size: 1.25rem;
    line-height: 1.35;
    text-decoration: none;
  }

  .foot-name:hover {
    text-decoration: none;
  }

  .foot-tagline {
    margin-top: var(--space-2xs);
    color: var(--color-ink-soft);
    font-family: var(--font-label);
    font-style: italic;
    font-size: var(--text-sm);
  }

  /* ── 工具行：一条发丝线压在最底 ──
     与 SiteFooterMinimal 的同名块保持同一套语言，两个页脚只差上面那行落款。
     三栏还在时这里的上边距是 clamp(2.5rem, 6vw, 4rem)，那是为了压住一块两百像素高的
     栏组；现在上面只有两行字，留那么多会把工具行推成孤零零的一条。 */
  .foot-utility {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: var(--space-md);
    margin-top: var(--space-xl);
    padding-top: var(--space-md);
    border-top: 1px solid var(--color-line);
    color: var(--color-ink-faint);
    font-size: var(--text-xs);
  }

  .foot-utility a {
    color: inherit;
    text-decoration: none;
    transition: color var(--duration-fast) var(--ease-out);
  }

  .foot-utility a:hover {
    color: var(--color-ink);
    text-decoration: underline;
  }
</style>
