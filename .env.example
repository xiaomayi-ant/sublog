# 知识图谱实体抽取（npm run graph:extract）用的 LLM 端点，OpenAI 兼容协议。
# 只在抽取脚本里读取；astro build 不联网、不依赖这些变量 —— 构建读的是
# 抽取导出的 data/graph.json，那个文件已经入库。
#
# 这个文件是模板。真要跑的话，直接把变量喂给那一条命令即可，不必落盘：
#   GRAPH_LLM_BASE_URL=https://api.deepseek.com/v1 \
#   GRAPH_LLM_API_KEY=$DEEPSEEK_API_KEY \
#   GRAPH_LLM_MODEL=deepseek-chat npm run graph:extract

# OpenAI 兼容端点，默认 https://api.openai.com/v1
GRAPH_LLM_BASE_URL=https://api.openai.com/v1
# 必填，没有它抽取脚本直接拒绝运行
GRAPH_LLM_API_KEY=
# 模型名，默认 gpt-4o-mini
GRAPH_LLM_MODEL=gpt-4o-mini
